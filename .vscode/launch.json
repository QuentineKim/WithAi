{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Current Lua File",
            "type": "lua-local",
            "request": "launch",
            "program": {
                "command": "lua"
            },
            "args": [
                "${file}"
            ],
            "console": "integratedTerminal"
        }
    ]
}